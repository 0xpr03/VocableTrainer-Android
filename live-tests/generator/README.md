Test data generator for VTA.

# Build
Install [Rust](https://www.rust-lang.org), run `cargo build --release`  
Under `target/release/generator` is now an executable you can run.
Use `--help` for a full list of parameters.
